# Run artifacts

This directory starts empty. `npm run game-agent -- build <brief.md> --id <run-id>` creates a run containing the brief, status, screenshots, and test-result directories. Planning, architecture, evidence, and critique for that game stay inside the same run.
